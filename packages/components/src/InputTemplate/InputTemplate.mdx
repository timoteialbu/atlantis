---
name: InputTemplate
menu: Components
route: /components/input-template
---

import { Playground, Props } from "docz";
import { ComponentStatus } from "@jobber/docx";
import { useState } from "react";
import { Button } from "../Button";
import { InputTemplate } from ".";

# InputTemplate

<ComponentStatus stage="pre" responsive="no" accessible="no" />

In Jobber a InputTemplate is used to …

## Simple Example

<Playground>
  {() => {
    const initial =
      "Yo {{ NAME }},\n\nThank you for your Biz! We made poor use of the money.\n\nLove,\nBusiness";

    const [text, setText] = useState(initial);

    return <>
      <InputTemplate value={text} onChange={handleChange} />
      <Button label="Reset" onClick={resetText} />
    </>;

    function handleChange(newText) {
      console.log("handleChange");
      setText(newText);
    }

    function resetText() {
      setText(initial);
    }

}}

</Playground>

## InputTemplate Properties

<Props of={InputTemplate} />
